# src/components/specific - Shared Domain Components

- Domain-aware components used by more than one feature.
- A component used by a single feature stays in that feature's folder. Move it here on its second
  use, not before.
- It may use domain types and hooks. It may not own a screen's flow.
