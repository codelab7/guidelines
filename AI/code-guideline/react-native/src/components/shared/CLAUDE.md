# src/components/shared - Generic Project UI

- Generic UI built for this project, with no domain knowledge.
- Examples: an empty state, a section header, a loading skeleton, a confirm sheet.
- It may know about the project's theme and spacing. It may not know about a feature.
- If it needs a domain type or a domain hook, it belongs in `specific/`.
