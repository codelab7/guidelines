# src/layouts/widgets - Shell Widgets

- Small pieces composed inside a shell section: a menu, an avatar button, a language sheet.
- Props in, callbacks out. No feature logic.
- This is shell content, not screen content. If a screen needs it, it belongs in `components/`.
